# domain 模块验证记录

| 属性 | 值 |
|------|-----|
| 模块 | `domain` |
| 验证状态 | `blocked` |
| 最近更新 | `2026-06-23` |
| 关联实现 | [01-domain-implementation.md](./01-domain-implementation.md) |

## 1. 验收目标

- `SecurityId.parse()` 能稳定处理沪深北港美常见格式
- 共享 DTO 可被上层模块直接消费
- 非法输入能抛出明确异常

## 2. 测试范围

- 单元测试：ID 解析、往返格式化、DTO 字段校验
- 集成测试：无
- 手动验证：示例输入输出检查

## 3. 验证清单

| 项 | 方法 | 结果 | 备注 |
|----|------|------|------|
| `600519` 解析为 `600519.SH` | 单元测试 | `not_started` |  |
| `300750` 解析为 `300750.SZ` | 单元测试 | `not_started` |  |
| `920799` 解析为 `920799.BJ` | 单元测试 | `not_started` |  |
| `600519.SH` 往返一致 | 单元测试 | `not_started` |  |
| 名称输入抛异常 | 单元测试 | `not_started` |  |
| DTO 可空字段符合设计 | 单元测试 | `not_started` |  |

## 4. 边界场景

- 空字符串
- 未知市场后缀
- 带前缀 `sh600519`
- 港美股显示后缀优先

## 5. 已知问题

- 本机默认 `python3` 为 3.9.6，未安装 Python 3.11，无法运行使用 `StrEnum` 的后端测试

## 6. 剩余风险

- 证券代码规则如果过于依赖前缀推断，后续扩市场时要补例外表

## 7. 验收结论

当前代码已实现，验证被本地 Python 版本阻塞；需在 Python 3.11 环境运行测试后更新结论。
